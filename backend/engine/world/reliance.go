package world

// ── Emergent reliance-cluster detection (D2 — detection only, no role type) ─────

// relianceScan is the cluster-detection step run after every apply phase.
// P1 STUB: tom.Belief.RelyOn has no concrete shape yet; this is intentionally a
// no-op. Replace with the full scan below once engine/tom formalizes RelyOn +
// Function. Scenario G is deferred — all other P1 acceptance criteria are
// unaffected.
//
// TODO(scenario-G): Activate once tom.Belief.RelyOn is formalized (see
// activation checklist in SPEC.md §Emergent-reliance).
func (w *World) relianceScan() { /* no-op stub */ }
