# Frontend Ecosystem Viewer — Implementation Plan

Concept: `frontend/SPEC.md` (viewer purpose + backend contract) + `docs/archive/world-readiness.md §5` (gap
audit). Wire contract: `docs/core/data-contracts.md §4/§10` (`WorldFrame`, Animal/Plant lifecycle events).
This file is the **roadmap** for replacing the hardcoded decorative mockup with a real ecosystem
render — terrain (river/grass/forest), flora (trees/bushes, growth stages), fauna (animals with
movement / attack / death motion), agents, ambient weather — plus the **asset pipeline** that feeds
it. It does not restate the SPECs; per-module detail lands in `frontend/SPEC.md` +
`frontend/src/render/SPEC.md` + `frontend/src/assets/SPEC.md` once the Open Questions resolve.

## 0. Decisions locked

- **Read-only viewer, SSE-driven.** All render input arrives via `GET /sse` (`WorldFrame` +
  lifecycle events) and REST initial-load. The frontend never invents simulation state; anything not
  streamed is not drawn (scent, cost fields). Env-off neutrality stays: no `WorldFrame` → the
  social-only render, byte-identical.
- **One shared world→canvas transform** (D11): agents, animals, flora, terrain cells map through ONE
  `buildTransform`; positions stay continuous floats; the terrain grid is a render *index*, entities
  never snap to cells.
- **Single source of truth = `useWorld` reducer.** Render functions are pure
  `(state, clockMs) → pixels` — animation takes an **explicit time input** (no hidden mutable
  clocks), so Vitest can assert any frame.
- **Sprite/style mapping is data, not code** (D10 mirror). `species → pose → asset` and
  `TerrainID → style` live in a **manifest module** (a typed data table + fallback chain), never in
  `if (species === 'wolf')` switches — species, ActionIDs, and TerrainIDs are open content
  (`fauna/SPEC.md`, `content/terrain.yaml`); a new species must render (via fallback) with **zero
  code edits**.
- **`canvasRenderer.ts` splits** (522 lines > ~400 rule): per-layer modules under
  `frontend/src/render/`, one SPEC for the folder. The uncommitted prototype's module-level
  `new Image()` singletons + hardcoded species/action substring switches are **redone** per the
  manifest/loader design (they violate the "no module-level mutable state" invariant).
- **Poses are a closed render vocabulary; ActionIDs are open.** The manifest maps open-content
  ActionIDs onto a small fixed pose set via an ordered pattern table + default — the render layer
  never enumerates ActionIDs. (Q2 RESOLVED: `idle|walk|run|eat|attack|dying`.)
- **Assets = PNG spritesheets** (Q1 RESOLVED, human choice over the SVG recommendation): one sheet
  per species under `frontend/public/assets/`, fixed frame size, one row per pose, N frames per
  row played at a manifest-declared fps; missing pose rows fall back (`pose→walk→idle→glyph`).
  Example sheets are generated by a checked-in dev script (`frontend/dev/generate-spritesheets.mjs`),
  so placeholders are reproducible and replaceable by real art without code changes.
- **Interactive camera** (Q6 RESOLVED, human choice): top-down view anchored to
  `RenderConfig.bounds` at load, then **wheel zoom in/out** (cursor-anchored), **drag pan**, and
  **click-to-follow**: selecting an entity locks the camera centre to it until deselect/manual pan.
  One camera state feeds the single shared transform (D11 unchanged).

## 1. Current baseline (what already works)

- **Data/state layer is done and ahead of the renderer**: `useWorld.ts` reduces `AgentFrame`
  (sparse per-agent deltas; replaced the removed full-roster `TickDone` frame, data-contracts §4) +
  `WorldFrame` into `WorldState.agents / animals / flora / climate`; `types.ts` carries every env
  shape (`AnimalState{pos,species,action,heading,stamina}`, `PlantState{pos,species,stage,width}`,
  `ClimateState`, `TerrainDelta`, `RenderConfig`). Bootstrap is snapshot-first with a Redis
  stream-cursor replay boundary + `world_revision` readiness marker (lossless late join, regen
  completion detection — the mechanism lives in `frontend/SPEC.md` §Bootstrap/§New-map flow).
  SSE connect/reconnect (`useSSE.ts`) works.
- **Render layer is a mockup**: `canvasRenderer.ts` draws a **hardcoded decorative map** (fake
  quadratic river, forests, buildings in canvas pixel coords, wrong "(1000,1000)" label) + agent
  dots; `buildTransform` auto-fits the agent/object bbox and ignores `RenderConfig`.
- **Uncommitted prototype (input, not settled design)**: 64×64 top-down SVGs in
  `frontend/public/assets/animals/` (deer walk/run/eat, wolf walk, tree, bush, agent) generated by
  root `generate_svgs.js`; `drawFlora`/`drawAnimals` that rotate by `heading` and dim by `stamina`;
  `frontend/mock-server.js` serving fake snapshot + SSE frames.
- **Backend WI-P4 is NOT implemented yet**: nothing emits `WorldFrame` today; `platform/api` has
  **no initial-terrain endpoint** (contract gap → Q5). Until WI-P4 lands, development runs against a
  mock (Q7) and the live viewer stays social-only (neutrality invariant).

## 2. Modules (new / changed)

| Module | Status | Content |
|---|---|---|
| `frontend/src/render/` (SPEC.md) | **new** (split of `utils/canvasRenderer.ts`) | `transform.ts` (world→px from camera state), `camera.ts` (zoom/pan/follow state machine, Q6), `terrain.ts`, `flora.ts`, `fauna.ts`, `agents.ts`, `ambient.ts`, `fx.ts` (transient spawn/death/attack/grow FX), `animator.ts`, `hitTest.ts` |
| `frontend/src/render/animator.ts` | **new** | pure interpolation helpers: adaptive position lerp between SSE frames (Q3), shortest-arc heading smoothing, spritesheet frame index + FX timeline evaluation — all `(state, clockMs) →` values |
| `frontend/src/assets/SPEC.md` + `manifest.ts` | **new** | the data tables: `SPECIES_SHEETS` (species → spritesheet url + frame size + pose rows/frames/fps + fallback chain species→category→glyph), `ACTION_POSE_RULES` (ordered ActionID pattern → pose), `TERRAIN_STYLE` (TerrainID → colour + default), `FX_DEFS` (durations/curves) |
| `frontend/src/assets/sprites.ts` | **new** | spritesheet loader/cache injected into render fns (preload from manifest, per-frame `drawImage` source rect, incomplete → glyph fallback) — replaces module-level `Image` singletons |
| `frontend/public/assets/` | **restructure** | PNG spritesheets per manifest layout (Q1): `fauna/<species>.png` (rows = poses), `flora/<species>.png` (frames = growth stages); terrain needs no files (flat colours). Old prototype `animals/*.svg` superseded |
| `frontend/dev/` | **new** | `mock-server.mjs` (Q7 — contract-parity dev fixture) + `generate-spritesheets.mjs` (placeholder asset generator); small SPEC.md |
| `frontend/src/hooks/useWorld.ts` | change | keep per-entity `prevPos/prevHeading + frameAtMs` for interpolation; append `AnimalDied`/`AnimalBorn`/`PlantSpawned`/`PlantDied` + attack-pose entry (Q4) into a pruned `fx[]` queue with timestamps |
| `frontend/src/components/WorldCanvas.tsx` | change | receive `animals/flora/climate/terrain/fx` slices; wheel/drag/click handlers → camera actions; RAF passes `performance.now()` into pure render fns; layer order terrain → flora → objects → animals → agents → fx → ambient |
| `frontend/src/types.ts` | change | `Pose`, `FxInstance`, `CameraState`, terrain grid state, interpolation fields |
| `frontend/src/utils/canvasRenderer.ts` | **deleted** | absorbed into `src/render/` |
| `frontend/mock-server.js` | **moved** | → `frontend/dev/mock-server.mjs`, rewritten to byte-match data-contracts §4 (Q7) |
| `backend/platform/api` (+`engine/world` wiring) | **contract gap** | initial full-terrain load (Q5) + `WorldFrame` emission itself (WI-P4, separate backend work — not this plan's scope, but Q5's answer edits its SPEC) |

## 3. Phases (each independently shippable; Vitest per phase; env-off neutrality holds throughout)

### FE-P1 — Render split + interactive camera (no new visuals) — ✅ shipped 2026-07-02
- Split `canvasRenderer.ts` → `src/render/*`; **delete the hardcoded decorative map** (fake
  river/forest/buildings/"1000" label). `render/camera.ts`: view anchored to
  `RenderConfig.bounds + pixelsPerUnit` at load (auto-fit fallback until fetched), wheel zoom
  (cursor-anchored, clamped), drag pan, click-to-follow selected entity (Q6 RESOLVED). Blocked by:
  Q6 ✅.

### FE-P2 — Asset pipeline + static ecosystem layers — ✅ shipped 2026-07-02
> Prototype `public/assets/animals/*.svg` + root `generate_svgs.js` deleted (user-approved,
> 2026-07-02, alongside FE-P3).
- `assets/manifest.ts` + `sprites.ts` + example PNG spritesheets in `public/assets/` generated by
  `frontend/dev/generate-spritesheets.mjs` (Q1 RESOLVED); `render/flora.ts` (stage-frame sprite
  scaled by width) + `render/fauna.ts` (spritesheet frame by species+pose, rotated by heading,
  dimmed by stamina) + agents stay coloured dots (Q8 RESOLVED). Poses chosen per-frame from
  `ACTION_POSE_RULES` (Q2 RESOLVED). Blocked by: Q1 ✅, Q2 ✅, Q8 ✅.

### FE-P3 — Motion (the "이동/공격/삭제 모션" phase) — ✅ shipped 2026-07-02
- `animator.ts`: position lerp + heading smoothing between successive frames (Q3); pose refinement
  (e.g. walk↔run by displacement rate) if Q3 resolves to lerp; `render/fx.ts` + reducer `fx[]`
  queue: spawn appear, death disappear (then removal), attack lunge/flash, flora stage-grow tween
  (Q4). Blocked by: Q3, Q4.

### FE-P4 — Terrain + ambient — ✅ shipped 2026-07-02
> Frontend side complete; the `/api/terrain` server route remains the pending backend SPEC
> delta (Q5). Wire shape consumed by `loadTerrain`:
> `{cell_size, orientation:'flat', size:{cols,rows}, terrain: string[] (offset(col,row) cols·rows), wear?: number[]}` (flat-top hex — `docs/plans/hex-grid.md`).
- `render/terrain.ts`: initial full grid (per Q5) + `terrain_delta` merge + `wear` trail overlay,
  styled by `TERRAIN_STYLE`; `render/ambient.ts`: day-night tint, temperature vignette, rain
  overlay, wind arrow (already specified in `frontend/SPEC.md §Ecosystem`). Blocked by: Q5.

### FE-P5 — Dev fixture + acceptance pass — ✅ shipped 2026-07-02
> All FE phases done; remaining work is backend-side: WI-P4 `WorldFrame` emission + the
> `/api/terrain` route.
- Mock/dev story per Q7 (contract-parity mock server or vitest fixtures only); run the full
  `frontend/SPEC.md` acceptance list + the new motion/asset ACs. Blocked by: Q7.

> Backend `WorldFrame` emission (WI-P4) is a **separate backend phase** (world → events → api);
> FE-P1..P5 are testable against fixtures without it.

## 4. Render purity & testability (the frontend's "determinism")

- Every render/animator fn is pure: `(slice, transform, clockMs) → draws/values`; sprite cache is
  injected, not global; `Math.random()` is forbidden in render paths (FX curves are
  time-parametric). Same state + same clock ⇒ same frame.
- Reducer stays the only writer of world state; `fx[]` entries carry absolute timestamps and are
  pruned by the reducer (on later events), never mutated by the render layer.
- Unknown species / ActionID / TerrainID **must** render via the fallback chain — a fixture with
  `species:"unknown_beast"` draws the category glyph, never throws.

## 5. Performance

- Budget: ~200 animals + ~500 plants + terrain grid (≈64×64 cells at 8.0 world units on the 512
  world) at 60fps RAF on a laptop.
- Terrain renders to an **offscreen canvas** re-blitted per frame; re-rasterized only on
  `terrain_delta`/wear change (cells are sparse deltas). Flora sorts/draws once per frame (mostly
  static); per-entity `drawImage` for fauna is fine at this count.
- SVG sprites decode once at preload (they rasterize on first `drawImage`; cache per size if it
  measures slow).

## 6. Open questions (all resolved by the human, 2026-07-02)

- **Q1 — Asset format & location** `RESOLVED: PNG spritesheets` (over the SVG recommendation).
  One PNG sheet per species under `frontend/public/assets/` (`fauna/<species>.png`,
  `flora/<species>.png`); fixed frame size, rows = poses, columns = animation frames, fps per pose
  in the manifest. Placeholder sheets generated by `frontend/dev/generate-spritesheets.mjs`;
  procedural glyphs remain the fallback chain's tail. The prototype SVGs in
  `frontend/public/assets/animals/` are superseded (delete when FE-P2 lands).
- **Q2 — Pose vocabulary + ActionID mapping** `RESOLVED: closed pose set + pattern table`.
  Poses = `idle | walk | run | eat | attack | dying`; ordered `ACTION_POSE_RULES` patterns
  (`hunt|attack→attack`, `flee→run`, `graze|eat|drink→eat`, locomotion→walk, default `idle`);
  new action content renders via patterns/default with zero code/asset edits (D10 mirror).
- **Q3 — Movement animation model** `RESOLVED: adaptive lerp`. Interpolate pos/heading from the
  previous to the latest frame over the measured inter-frame interval; heading via shortest arc;
  snap is the degenerate case when frames outpace RAF. Walk↔run refinement may read displacement
  rate.
- **Q4 — Transition FX** `RESOLVED: reducer-owned fx[] queue; fade+corpse ~1.5 s; pose-triggered
  attack FX`. Lifecycle events (`AnimalBorn/AnimalDied/PlantSpawned/PlantDied`) and attack-pose
  entry enqueue `{kind, at, pos, meta}`; render evaluates time-parametrically; the reducer prunes
  expired entries. No backend contract change.
- **Q5 — Terrain initial load** `RESOLVED: new REST GET /api/terrain` + flat per-cell colour.
  Response `{cell_size, orientation:'flat', size:{cols,rows}, terrain:[TerrainID-index offset array], wear?}` once at
  connect; SSE `terrain_delta` keeps it current. **Pending backend SPEC delta**: add the route to
  `backend/platform/api/SPEC.md` + a world terrain-grid read accessor when the WI-P4/backend phase
  starts (read-only, no god-view; edge blending parked as polish).
- **Q6 — Camera** `RESOLVED: interactive top-view` (human choice, supersedes the fixed-view
  recommendation). Anchor to `RenderConfig.bounds` at load; wheel **zoom in/out** (cursor-anchored,
  clamped to sane min/max); drag pan; **click an entity → select + camera follows it** until
  deselect or manual pan/zoom-out breaks the lock. One `CameraState` feeds the single shared
  transform.
- **Q7 — Dev/mock story** `RESOLVED: keep the mock server, SPEC'd`. Move to
  `frontend/dev/mock-server.mjs`; it MUST emit byte-shape-identical data-contracts §4 events
  (`AgentFrame`, `WorldFrame`, lifecycle events — `TickDone` is no longer emitted) and serve
  `/api/snapshot` + `/api/terrain` with the
  real casing/paths (today's mock drifts: `world.Agents` casing, `/mock-api` paths — rewrite).
- **Q8 — Agent representation** `RESOLVED: coloured dot + cluster colour/aura stays`. Sprite
  treatment for agents is revisited after motion ships.

## 7. Build order

FE-P1 (split+camera) → FE-P2 (assets+static layers) → FE-P3 (motion/FX) → FE-P4 (terrain+ambient)
→ FE-P5 (fixture+acceptance). Backend WI-P4 (`WorldFrame` emission + Q5 endpoint) can land any time
≥ FE-P1; the viewer upgrades live without frontend changes thanks to env-off neutrality.
